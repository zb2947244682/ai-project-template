# 临时文件目录

本目录用于存放 AI 运行过程中生成的临时文件和脚本。

## 目录结构

```
temp/
├── README.md
├── docs/          # 临时文档（阅后即删）
└── scripts/       # AI 生成的临时脚本
```

## 规则

- 所有临时脚本必须放在 `temp/scripts/` 目录下
- 所有临时文档必须放在 `temp/docs/` 目录下
- 禁止在项目根目录放置临时文件
- 本目录下的文件不会被 git 跟踪
- 定期清理临时文件
